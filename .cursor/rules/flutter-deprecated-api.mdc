---
alwaysApply: true
---
# Flutter弃用API检查规则

## 🚫 严格禁止的弃用API

### 1. 颜色透明度设置
```dart
// ❌ 绝对不要使用
Colors.red.withOpacity(0.5)
color.opacity

// ✅ 必须使用
Colors.red.withValues(alpha: 0.5)
color.a
```

### 2. 字符串插值优化
```dart
// ❌ 避免使用（单个变量时）
'${variable}'

// ✅ 推荐使用
'$variable'
```

## 🔧 自动修复命令

### 批量修复withOpacity
```bash
find lib -name "*.dart" -exec sed -i '' 's/\.withOpacity(\([^)]*\))/\.withValues(alpha: \1)/g' {} \;
```

### 批量修复color.opacity
```bash
find lib -name "*.dart" -exec sed -i '' 's/\.opacity/\.a/g' {} \;
```

### 验证修复结果
```bash
flutter analyze
```

## 📋 开发检查清单

### 代码质量检查
1. 运行 `flutter analyze` 确保无问题
2. 运行 `flutter test` 确保测试通过
3. 检查是否使用了弃用API
4. 遵循代码风格规范

### IDE配置
- 启用Dart Analysis
- 配置代码风格检查
- 设置自动导入

## 🎯 最佳实践

### 编写代码时
- 注意IDE的实时提示
- 使用正确的API
- 遵循代码风格规范

### 提交前检查
```bash
flutter analyze
flutter test
```

## ⚠️ 重要提醒

1. **代码规范是强制性的**，不是建议
2. **所有弃用API必须立即修复**，不允许提交
3. **提交前必须通过所有自动检查**
4. **遇到问题查看开发指南**

## 📁 相关文件

- [analysis_options.yaml](mdc:analysis_options.yaml) - Flutter linting配置
- [DEVELOPMENT_GUIDE.md](mdc:DEVELOPMENT_GUIDE.md) - 开发指南
# Flutter弃用API检查规则

## 🚫 严格禁止的弃用API

### 1. 颜色透明度设置
```dart
// ❌ 绝对不要使用
Colors.red.withOpacity(0.5)
color.opacity

// ✅ 必须使用
Colors.red.withValues(alpha: 0.5)
color.a
```

### 2. 字符串插值优化
```dart
// ❌ 避免使用（单个变量时）
'${variable}'

// ✅ 推荐使用
'$variable'
```

## 🔧 自动修复命令

### 批量修复withOpacity
```bash
find lib -name "*.dart" -exec sed -i '' 's/\.withOpacity(\([^)]*\))/\.withValues(alpha: \1)/g' {} \;
```

### 批量修复color.opacity
```bash
find lib -name "*.dart" -exec sed -i '' 's/\.opacity/\.a/g' {} \;
```

### 验证修复结果
```bash
flutter analyze
```

## 📋 开发检查清单

### 代码质量检查
1. 运行 `flutter analyze` 确保无问题
2. 运行 `flutter test` 确保测试通过
3. 检查是否使用了弃用API
4. 遵循代码风格规范

### IDE配置
- 启用Dart Analysis
- 配置代码风格检查
- 设置自动导入

## 🎯 最佳实践

### 编写代码时
- 注意IDE的实时提示
- 使用正确的API
- 遵循代码风格规范

### 提交前检查
```bash
flutter analyze
flutter test
```

## ⚠️ 重要提醒

1. **代码规范是强制性的**，不是建议
2. **所有弃用API必须立即修复**，不允许提交
3. **提交前必须通过所有自动检查**
4. **遇到问题查看开发指南**

## 📁 相关文件

- [analysis_options.yaml](mdc:analysis_options.yaml) - Flutter linting配置
- [DEVELOPMENT_GUIDE.md](mdc:DEVELOPMENT_GUIDE.md) - 开发指南
